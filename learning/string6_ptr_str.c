#include <stdio.h>
int main(){
    // char *canchange="hello world";
    // puts(canchange);
    // canchange="hello";
    // puts(canchange);
    // canchange="world";
    // puts(canchange);

    // char notchange[]="hello world";
    // puts(notchange);
    // notchnge="hello";
    // puts(notchange);
    // notchnage="world";
    // puts(notchange);
    return 0;
}
// if we define string as any pointer then we can change its value 
//but if we define string such as array then we cannot change its value
